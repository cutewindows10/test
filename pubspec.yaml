name: testing
description: "A new Flutter project."

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  image_picker: ^1.1.2  # Updated to the latest version
  shared_preferences: ^2.2.3  # Updated to the latest version
  universal_html: ^2.2.4  # Updated to the latest version
  cupertino_icons: ^1.0.6
  camera: ^0.11.0+1  # Updated to the latest version
  path_provider: ^2.1.3  # Updated to the latest version
  
  path: ^1.9.0  # Ensuring path is at least 1.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0  # Updated to the latest version

flutter:
  uses-material-design: true

  assets:
    - assets/images/logo.png
